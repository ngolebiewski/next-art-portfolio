{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "PRISMA_GENERATE": "true"
    },
    "commands": [
      "prisma generate"
    ]
  }
}
